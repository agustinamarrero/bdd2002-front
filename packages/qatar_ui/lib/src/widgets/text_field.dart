import 'package:flutter/material.dart';

class QATARTextField extends StatelessWidget {
  const QATARTextField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TextField();
  }
}
